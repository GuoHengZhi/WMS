# 海外仓系统（WMS）完整产品需求文档（PRD）

> 对标系统：威汇赢海外仓（http://62.234.211.40:9000，Vue2 + Element UI 单页应用，2026-09 逐页实测调研）
> 本文档以实测功能为基准 1:1 对齐，并按通用海外仓/WMS 行业标准补全系统未直接暴露但完整产品必需的能力，补全部分统一标注【补充】，推断内容标注（推断）。

---

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 海外仓系统（WMS）产品需求文档 |
| 文档编号 | PRD-WMS-001 |
| 版本号 | V1.0 |
| 状态 | 待评审 |
| 作者 | 产品经理 |
| 对标系统 | 威汇赢海外仓（客户端实测 + 行业标准补全） |
| 目标读者 | 研发、测试、UI 设计、运营、仓库业务方 |

### 0.1 修订记录

| 版本 | 日期 | 修订人 | 修订说明 |
| --- | --- | --- | --- |
| V0.1 | 2026-09-08 | 产品经理 | 基于对标系统逐页调研初稿 |
| V1.0 | 2026-09-08 | 产品经理 | 补全角色/权限/计费/仓库端能力/数据模型，形成完整评审稿 |

---

## 1. 产品概述

### 1.1 背景

跨境电商卖家在海外销售时，商品需要提前备货到目的国仓库（头程运输），由本地仓库完成收货、上架、库存管理与终端配送（一件代发）。自建仓成本高，第三方海外仓服务商（如"威汇赢海外仓"，当前已运营韩国仓）需要一套 WMS（Warehouse Management System）为卖家提供在线的仓储服务门户。

现有系统为"客户端 + 仓库端"形态中的客户端（卖家端）+ 后台管理混合形态：卖家登录后可完成入库预报、出库下单、库存查询、账单查看等操作；仓库方的确认/揽收/上架/出货等动作在后台或人工流程中完成。本 PRD 以实测客户端功能为基准，同时补全仓库操作端（PDA 作业）能力，构成完整可交付的海外仓 WMS 产品。

### 1.2 产品定位

- **面向对象**：海外仓服务商（运营方）与其服务的跨境电商卖家（客户）。
- **产品形态**：B 端 Web 多租户系统。卖家以客户账号（如 VIP0157、SHEN）登录；运营方按角色登录管理。
- **核心价值**：卖家"下单式"使用仓储服务——预报入库 → 库存可视化 → 一件代发/换标/退货在线下单 → 仓库执行 → 费用按单计费、账单可查。
- **业务模式**：仓储 + 代发服务收费，计费币种为人民币（￥），支持多仓（当前实测为"威汇赢韩国仓"，架构上必须支持多仓多国家扩展）。

### 1.3 产品目标

1. **对齐现有系统**：菜单结构、页面、字段、按钮、状态流与"威汇赢海外仓"实测结果一一对应，保证业务平滑迁移。
2. **补全闭环**：补齐仓库操作端（PDA 收货上架、拣货复核打包称重、面单打印、盘点/调整/移位、库位体系）与计费规则配置，使系统具备独立完整交付能力。
3. **可扩展**：支持多仓、多国家、多币种预留、多电商平台对接（Amazon 优先）。
4. **可验收**：每条需求均给出字段级定义、业务规则与验收要点，可直接指导开发与测试用例编写。

### 1.4 成功指标

| 指标类别 | 指标 | 目标值（上线 3 个月） |
| --- | --- | --- |
| 效率 | 入库单从创建到"已完成"平均时长 | ≤ 3 天（自发头程，韩国仓） |
| 效率 | 一件代发订单从"待下架"到"已出货"平均时长 | ≤ 1 个工作日 |
| 准确率 | 收货上架库存准确率 | ≥ 99.5% |
| 准确率 | 拣货错发率 | ≤ 0.1% |
| 财务 | 订单账单自动生成率 | ≥ 95%（操作费类） |
| 体验 | 客户端列表查询响应（P95） | ≤ 2s |
| 使用 | 月活跃客户账号数 | 按运营目标设定，系统无技术瓶颈 |

### 1.5 范围说明

- **范围内（In Scope）**：实测 8 大模块 29 个页面全部功能；全局框架（登录、多标签页、i18n、消息）；【补充】仓库操作端（PDA）；【补充】计费规则与账单闭环；店铺授权与订单同步（Amazon 优先）。
- **范围外（Out of Scope）**：头程国际运输管理（干线物流 TMS）、采购管理、多级经销商体系、移动端 App（二期评估）、海外仓自营 WMS 硬件采购方案。

---

## 2. 名词解释

| 名词 | 解释 |
| --- | --- |
| 头程 | 卖家将商品从国内发往海外仓的跨境运输段。本系统中入库单"交货方式"包含"自发头程"（卖家自己安排运输）与"中转代发"（通过服务商中转）。 |
| 一件代发（Dropshipping） | 卖家店铺出单后，由海外仓按订单直接向终端消费者发货，卖家无需备货到本地。对应系统"一件代发"模块。 |
| FBA 换标 | 卖家将海外仓库存的 Amazon FBA 商品（FnSku 标签）更换为新 FnSku 标签后发往亚马逊仓库，对应"换标管理"模块。 |
| RMA | Return Merchandise Authorization，退货授权。本系统"退货管理"即处理客户发起的退回入库与处置流程。 |
| SKU | Stock Keeping Unit，最小库存单位。系统内每个客户的产品唯一标识。 |
| FnSku | Amazon FBA 商品条码标识（X00 开头条码 + 标签），换标单的新旧 FnSku 字段即指此。 |
| ASN | Advance Shipping Notice，入库预报单。本系统"创建入库"即生成 ASN（入库单）。 |
| 波次 | 将多张出库单按规则合并成一个仓库拣货批次。【补充】仓库端支持波次拣货。 |
| 面单 | 承运商运输标签。系统"打印状态/打印"按钮、"导出打单数据"均围绕面单。 |
| 认领 | 仓库收到无主货物（无预报/无法匹配）时挂出待认领列表，客户确认归属后转为入库。 |
| 服务渠道 | 入库/退货单选择的服务类型（如普通入库、加急）。 |
| 到仓方式 | 货物送达仓库的方式（如快递、卡车、自送）。（推断，以下拉选项由运营配置） |
| 可库库存（可用库存） | 实际在库且未被出库单占用的库存。实测"库存查询"列名为"可库库存"。 |
| 库位 | 仓库内货位编码（如 A-01-02-03）。【补充】库位体系为仓库端精细化管理基础。 |
| PDA | 手持扫码终端，仓库操作员用于收货、上架、拣货、盘点等扫码作业。【补充】 |
| 打单数据 | 供面单打印使用的结构化数据导出（实测"导出打单数据"按钮）。 |

---

## 3. 用户与角色

### 3.1 角色定义

实测系统仅暴露客户端视角（账号 SHEN/VIP0157 均为客户侧账号），以下角色体系为按业务闭环补全：

| 角色 | 说明 | 职责 | 权限边界 |
| --- | --- | --- | --- |
| 客户/卖家（Customer） | 系统主租户，按"用户名"维度隔离数据（实测 VIP0157） | 维护产品/店铺/SKU 映射；创建入库/出库/换标/退货单；查看库存、账单；认领无主货 | 只能访问自己的数据；不能操作仓库执行类动作 |
| 仓库操作员（Warehouse Operator）【补充】 | 仓库一线作业人员 | PDA 收货、上架、拣货、复核、打包、称重、贴标、盘点执行 | 按所属仓库授权；只看本仓数据；无财务权限 |
| 仓库管理员（Warehouse Manager）【补充】 | 仓库现场管理 | 确认入库单、波次管理、异常处理（无主货挂认领）、库存调整审批、出货交接 | 本仓全部操作 + 单据审核 |
| 客服（Customer Service）【补充】 | 运营方客服 | 处理客户咨询、订单异常跟进、认领协调、换标/退货进度跟进 | 全数据只读 + 有限的单据备注/取消权限 |
| 财务（Finance）【补充】 | 运营方财务 | 计费规则维护、账单生成与对账、收款核销、银行账户管理 | 财务中心全部；其他模块只读 |
| 超级管理员（Super Admin）【补充】 | 系统管理 | 用户/角色/权限管理、仓库/服务商/渠道等基础数据维护、系统参数 | 全部功能 |

### 3.2 权限矩阵（【补充】按 RBAC 模型设计）

图例：✅ 可操作　👁 只读　— 无权限

| 功能模块/页面 | 客户/卖家 | 仓库操作员 | 仓库管理员 | 客服 | 财务 | 超级管理员 |
| --- | --- | --- | --- | --- | --- | --- |
| 我的店铺-店铺订单 | ✅ | — | 👁 | 👁 | — | ✅ |
| 我的店铺-店铺授权 | ✅ | — | — | 👁 | — | ✅ |
| 我的店铺-SKU映射 | ✅ | — | — | 👁 | — | ✅ |
| 基本信息-产品管理 | ✅ | 👁（本仓相关） | 👁 | 👁 | — | ✅ |
| 产品审核 | — | — | ✅ | — | — | ✅ |
| 入库-创建入库 | ✅ | — | — | — | — | ✅ |
| 入库-待揽货/待确认/所有单 | 👁（本人） | ✅（本仓执行） | ✅（本仓） | 👁 | 👁 | ✅ |
| 入库-认领管理 | ✅（认领） | ✅（挂认领） | ✅（本仓） | 👁 | — | ✅ |
| 一件代发-创建出库 | ✅ | — | — | — | — | ✅ |
| 一件代发-待下架/待出货/已出货/所有单 | 👁（本人） | ✅（本仓执行） | ✅（本仓） | 👁 | 👁 | ✅ |
| 换标管理（全部页面） | ✅（创建/查看本人） | ✅（本仓执行） | ✅（本仓） | 👁 | — | ✅ |
| 退货管理（全部页面） | ✅（创建/查看本人） | ✅（本仓执行） | ✅（本仓） | 👁 | — | ✅ |
| 库存-库存明细 | 👁（本人） | 👁（本仓） | ✅（本仓） | 👁 | 👁 | ✅ |
| 库存-库存查询 | 👁（本人） | 👁（本仓） | ✅（本仓） | 👁 | 👁 | ✅ |
| 库存-盘点/调整/移位【补充】 | — | ✅（盘点执行） | ✅（审批/调整） | — | 👁 | ✅ |
| 财务-银行账户 | ✅（本人） | — | — | — | ✅ | ✅ |
| 财务-订单账单 | 👁（本人） | — | — | 👁 | ✅ | ✅ |
| 计费规则配置【补充】 | — | — | 👁 | — | ✅ | ✅ |
| 系统管理（用户/角色/仓库）【补充】 | — | — | — | — | — | ✅ |

---

## 4. 总体业务流程

### 4.1 端到端业务全景（文字化流程图）

```
卖家（客户账号）
  │ ①维护基础数据：产品管理（Sku/中英文名/分类/长宽高/重量/规格）→ 提交 → 客服/运营审核 → 已审核
  │ ②授权店铺（店铺授权）→ 系统同步店铺订单（店铺订单）→ 配置 SKU映射 → 订单映射成功 → 可选"审核"转为一件代发出库单
  ▼
③创建入库（预报 ASN，单号 RK+日期+流水）→ 货物到仓 → 仓库揽收（记"揽收"时间）→ 仓库确认（待确认列表）→ PDA 扫码上架（记"上架"时间）→ 库存增加（库存明细/库存查询）
  │                                        └→ 无法匹配预报 → 挂"认领管理"，客户认领后并入流程
  ▼
④卖家创建出库（一件代发：选仓库/国家/产品+数量，实时校验可库库存）→ 待下架（仓库拣货下架）→ 待出货（打印面单、打包、称重、交运，记"单号"=承运商追踪号）→ 已出货 → 库存扣减
  ▼
⑤（可选）换标：创建换标（Fba仓库+发货仓库+派送渠道+FnSku→新FnSku）→ 待拣货 → 待贴标 → 待发货 → 已发货
  ▼
（可选）退货：创建退货（退货预报）→ 退货待揽收 → 退货待上架（入仓）→ 退货待处理（质检/定处置）→ 退货待完成 → 退货已完成（退货完成时间）
  ▼
⑥计费：各业务节点触发计费事件 → 按计费规则生成订单账单（关联单号=入库单/出库单/换标单/退货单号）→ 客户在"订单账单"查询 → 扣费/支付（余额模型，推断）→ 状态更新
  ▼
⑦财务：银行账户维护收款信息 → 账单对账核销
```

### 4.2 入库流程（主状态机）

```
[客户创建入库单] → 待揽货（已预报）
      → [仓库揽收]（记录揽收时间）→ 待确认（已揽收待确认）
      → [仓库确认/清点] → 上架中【补充 PDA 扫码上架】
      → [上架完成]（记录上架时间）→ 已完成 → 库存入账
分支：
  - 无法匹配预报 → 无主货（认领中）→ [客户认领] → 回到揽收后流程
  - [客户/运营取消] → 已取消（仅"待揽货"可取消）
```

| 状态 | 触发动作 | 记录字段 |
| --- | --- | --- |
| 待揽货 | 客户提交入库单 | 创建时间 |
| 待确认 | 仓库揽收 | 揽收时间 |
| 上架中 | 仓库确认开始上架（推断状态名） | — |
| 已完成 | 上架完成 | 上架时间 |
| 无主货（认领中） | 仓库收货无法匹配 | 挂入认领管理 |
| 已取消 | 客户/运营取消 | 取消时间、操作人 |

### 4.3 一件代发（出库）流程（主状态机）

```
[客户创建出库单] → 待下架
      → [仓库下架拣货] → 待出货
      → [打印面单（打印状态→已打印）→ 打包/称重【补充】→ 获取承运商单号 → 交运]
      → 已出货（记录发货时间）
分支：
  - 库存不足 → 创建时拦截（选择产品时实时显示"库存"列）
  - [客户/客服取消] → 已取消（仅"待下架"可取消）
```

| 状态 | 触发动作 | 关键字段变化 |
| --- | --- | --- |
| 待下架 | 出库单创建成功 | 创建时间 |
| 待出货 | 仓库完成下架拣货 | 发货单号（仓库内部单号） |
| 已出货 | 打包交运完成 | 承运商单号（"单号"列）、发货时间、物流渠道 |
| 已取消 | 取消 | — |

### 4.4 换标流程（主状态机）

```
[客户创建换标单] → 待拣货（待下架）
      → [仓库拣货下架] → 待贴标
      → [仓库贴新FnSku标签] → 待发货
      → [交运发出]（记录发货时间）→ 已发货
```

### 4.5 退货流程（主状态机）

```
[客户创建退货单] → 退货待揽收
      → [仓库揽收入仓] → 退货待上架
      → [上架入库存] → 退货待处理（质检/定处置方式：重新上架/销毁/退回等）
      → [处置执行] → 退货待完成
      → [确认完成] → 退货已完成（记录退货完成时间）
```

### 4.6 计费流程【补充】

```
业务节点触发计费事件（入库上架完成 / 出库已出货 / 换标已发货 / 退货完成 / 仓储费月结）
      → 系统按"计费规则配置"计算费用
      → 生成订单账单（账单编号、关联单号、费用金额￥、费用时间、状态=待支付）
      → 客户余额扣费 / 线下打款后财务核销
      → 账单状态 → 已支付；扣费失败 → 欠费提醒（消息中心）
```

---

## 5. 功能需求总览（页面清单）

实测菜单树：8 个一级模块，共 29 个菜单页面（下表"来源"列标注实测/补充）。

| # | 一级模块 | 菜单页面 | 页面路由 | 来源 |
| --- | --- | --- | --- | --- |
| 1 | 我的店铺 | 店铺订单 | /shop/order | 实测 |
| 2 | 我的店铺 | 店铺授权 | /shop/authorize | 实测 |
| 3 | 我的店铺 | SKU映射 | /shop/sku_mapping | 实测 |
| 4 | 基本信息 | 产品管理 | /basic/product | 实测 |
| 5 | 基本信息 | 产品分类管理【补充】 | /basic/category | 补充 |
| 6 | 入库管理 | 认领管理 | /inbound/claim | 实测 |
| 7 | 入库管理 | 创建入库 | /inbound/create | 实测 |
| 8 | 入库管理 | 待揽货 | /inbound/pickup | 实测 |
| 9 | 入库管理 | 待确认 | /inbound/confirm | 实测 |
| 10 | 入库管理 | 所有单 | /inbound/all | 实测 |
| 11 | 一件代发 | 创建出库 | /outbound/create | 实测 |
| 12 | 一件代发 | 待下架 | /outbound/outbound-djh | 实测 |
| 13 | 一件代发 | 待出货 | /outbound/outbound-dfh | 实测 |
| 14 | 一件代发 | 已出货 | /outbound/outbound-yfh | 实测 |
| 15 | 一件代发 | 所有单 | /outbound/outbound-all | 实测 |
| 16 | 换标管理 | 创建换标 | /fbarebrand/create | 实测 |
| 17 | 换标管理 | 待拣货（菜单名"待下架"） | /fbarebrand/rebrand-djh | 实测 |
| 18 | 换标管理 | 待贴标 | /fbarebrand/rebrand-dtb | 实测 |
| 19 | 换标管理 | 待发货 | /fbarebrand/rebrand-dfh | 实测 |
| 20 | 换标管理 | 已发货 | /fbarebrand/rebrand-yfh | 实测 |
| 21 | 退货管理 | 创建退货 | /return/create | 实测 |
| 22 | 退货管理 | 退货待揽收 | /return/return-dlh | 实测 |
| 23 | 退货管理 | 退货待上架 | /return/return-dsj | 实测 |
| 24 | 退货管理 | 退货待处理 | /return/return-dcl | 实测 |
| 25 | 退货管理 | 退货待完成 | /return/return-dwc | 实测 |
| 26 | 退货管理 | 退货已完成 | /return/return-ywc | 实测 |
| 27 | 库存管理 | 库存明细 | /stock/stock-detail | 实测 |
| 28 | 库存管理 | 库存查询 | /stock/inquiry | 实测 |
| 29 | 库存管理 | 库位管理【补充】 | /stock/location | 补充 |
| 30 | 库存管理 | 盘点/调整/移位【补充】 | /stock/adjust | 补充 |
| 31 | 财务中心 | 银行账户 | /finance/bankInfo | 实测 |
| 32 | 财务中心 | 订单账单 | /finance/payment | 实测 |
| 33 | 财务中心 | 计费规则【补充】 | /finance/billing-rule | 补充 |
| 34 | 仓库操作端（PDA）【补充】 | 收货/上架/拣货/打包/盘点 | /pda/* | 补充 |

---

## 6. 功能需求明细

### 6.0 全局框架需求（实测）

| 编号 | 需求 | 说明 | 验收要点 |
| --- | --- | --- | --- |
| G-01 | 登录 | 账号 + 密码 + 图形验证码登录；提供"12小时内免登录"选项，勾选后 12 小时内免重复登录 | 验证码错误登录失败；免登录期间会话保持；过期后强制重新登录 |
| G-02 | 顶部导航 | 左侧一级/二级菜单树；面包屑导航（如"入库管理 > 所有单"） | 菜单展开/收起正常；面包屑与当前页面一致 |
| G-03 | 多标签页工作区 | 已访问页面以顶部 Tab 形式打开（如"待下架、个人设置、所有单"），可切换/关闭 | 打开新页面新增 Tab；关闭 Tab 不影响其他页面状态 |
| G-04 | 多语言 | 顶部"语言"按钮切换中文/英文，全站 i18n | 切换后页面文案实时切换；i18n key 必须全覆盖（实测存在 "forecast.filter.placeholder.status"、"order.filter.placeholder.status" 未翻译的漏翻缺陷，新系统不允许出现） |
| G-05 | 用户菜单与消息 | 顶部显示当前用户（如 SHEN）、消息中心（未读数角标，实测为 0）、个人设置页 | 个人设置 Tab 可打开（改密码/偏好设置）；消息未读数实时更新 |
| G-06 | 通用列表能力 | 所有列表页支持：分页（默认 10 条/页，可选）、列排序（用户名/仓库名/时间等列点击排序）、多选 checkbox、导出 Excel | 排序按点击列升降切换；导出遵循当前筛选条件；导出文件格式 xlsx 且列名与页面一致 |
| G-07 | 数据隔离 | 全部列表默认按当前登录用户的"用户名"维度过滤，多租户隔离 | 用户 A 永远查不到用户 B 的数据；仓库/管理员角色按仓库维度 |
| G-08 | 表单校验 | 必填项红星标注，提交前端校验 + 后端校验，错误信息就近提示 | 空必填项提交被拦截且提示明确 |
| G-09 | 币种 | 所有费用金额显示为人民币 ￥ | 金额格式统一两位小数 |

---

### 6.1 我的店铺（/shop/*）

#### 6.1.1 店铺订单（/shop/order）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 展示已授权店铺从电商平台（Amazon 等）同步下来的店铺订单；卖家对订单执行映射检查与审核，审核通过后可转为一件代发出库单 |
| 筛选条件 | ① 选择店铺（下拉，"全部"）② 状态（下拉，全部；i18n key: order.filter.placeholder.status）③ 关键字（文本，多个英文逗号 ',' 分隔，匹配订单编号/平台单号等） |
| 操作按钮 | 查询、同步订单（手动触发店铺订单同步）、重新执行映射（选中行后启用）、审核（选中行后启用） |
| 列表字段 | 序号、用户名、店铺名称、订单编号、下单时间（实测可见列；【补充】建议扩展：平台订单号、收件人、商品明细、映射状态、订单金额） |
| 交互逻辑 | 1) 勾选 1 行或多行后"重新执行映射""审核"按钮由 disabled 变可用；2) "同步订单"按店铺批量拉取平台订单；3) 审核通过 → 进入一件代发待下架队列 |

**业务规则**

1. 只有"店铺授权"中授权成功的店铺才可同步。
2. 订单同步频率：系统定时（建议 ≥ 每 30 分钟）+ 手动触发。
3. 订单必须完成 SKU 映射后才能审核；未映射订单审核时拦截并提示缺失 SKU。
4. 审核通过订单自动生成一件代发出库单（默认仓库=店铺配置仓库（推断），商品数量=映射后的系统 Sku 数量）。
5. 重复同步按平台订单号幂等去重。

**异常与边界**：店铺 token 失效 → 同步失败并在列表标注错误原因（【补充】店铺授权管理中提示重新授权）；重复审核 → 幂等拦截；映射后平台 Sku 数量与系统 Sku 数量不一致 → 显示映射比例。

**验收要点**：筛选/查询正确；同步幂等；未映射订单无法审核；审核后可在一件代发"待下架"看到新单；"重新执行映射"仅对映射失败订单有效。

#### 6.1.2 店铺授权（/shop/authorize）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 管理卖家在系统中的电商平台店铺授权信息（店铺名、授权凭据等） |
| 筛选条件 | 店铺中文/英文名称（模糊） |
| 操作按钮 | 查询、新增 |
| 列表字段 | 序号、用户名、店铺名称、英文名称、排序、备注、操作（编辑/删除（推断）） |
| 交互逻辑 | 新增弹窗：选择平台（Amazon 等【补充】）、店铺中文名、英文名、授权方式（OAuth 跳转/填入凭据）、排序、备注 |

**业务规则**：同一用户下店铺名称唯一；授权状态需展示（已授权/已失效）（【补充】实测页面未见授权状态列，建议增加"授权状态"与"失效时间"）；删除店铺需二次确认且校验该店铺存在未审核订单。

**验收要点**：新增后列表刷新可见；名称可中英双语搜索；授权失效店铺同步订单时给出明确错误。

#### 6.1.3 SKU映射（/shop/sku_mapping）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 维护"平台产品 Sku → 系统 Sku"映射关系，是店铺订单自动转出库单的核心配置 |
| 筛选条件 | 用户名、店铺名称、平台/系统产品 sku（文本）、系统 Sku 状态（下拉） |
| 操作按钮 | 查询、新增、批量删除（选中后启用）、导入、导出 |
| 列表字段 | 序号、用户名、店铺名称、平台产品Sku、系统Sku、系统Sku数量、系统Sku状态、创建时间、操作（编辑/删除） |
| 交互逻辑 | 新增：选店铺 → 输入平台产品 Sku → 输入系统 Sku（可校验存在性）→ 系统Sku数量（1 个平台 Sku 对应 N 个系统 Sku 的组合数量，支持拆件）→ 保存 |

**业务规则**

1. 同一店铺 + 平台产品 Sku 唯一（一个平台 Sku 可映射到多个系统 Sku 行，按行管理）。
2. 系统 Sku 必须为当前用户"产品管理"中存在的产品；状态被禁用的产品映射时提示。
3. 批量删除需二次确认；导入模板含表头说明，导入错误行提示行号与原因。
4. 导出遵循当前筛选条件。

**异常与边界**：映射删除后，存量未审核订单"重新执行映射"时提示无映射；系统 Sku 名称变更不影响映射（按 Sku 编码关联）。

**验收要点**：四项筛选生效；新增/导入的去重校验；批量删除仅删除选中行；导出内容与列表一致。

---

### 6.2 基本信息（/basic/*）

#### 6.2.1 产品管理（/basic/product）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 客户维护自己的产品（SKU）档案：名称、分类、尺寸重量、规格，作为入库预报/出库/库存/计费的基础 |
| 筛选条件 | 用户名、Sku 或 FnSku（多个英文逗号 ',' 隔开）、中文或英文名称（模糊）、状态（默认全部）、审核状态（默认全部） |
| 操作按钮 | 查询、新增、审核（选中后启用）、导入、导出 |
| 列表字段 | 序号、用户名、Sku、产品图片、中文名称、英文名称、所属分类、长*宽*高(cm)、重量(kg)、规格、状态（启用/禁用开关）、审核状态、操作（复制、更多） |

**列表实测样例**：VIP0157 / S0007 / 眼影盘 / 팔레트/02（韩文英文名）/ 化妆品 / 1*1*1 / 1 / 1 / 启用 / 已审核 —— 说明：英文名称与分类均为多语言文本，分类包含 化妆品、电子产品、保健用品、其他品类（实测）。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| Sku | 文本，必填，唯一 | 用户内唯一的产品编码 |
| 产品图片 | 图片 | 支持多张（建议 ≤5 张），列表显示第一张 |
| 中文名称/英文名称 | 文本，必填 | 英文名称支持任意语言文本（实测含韩文） |
| 所属分类 | 下拉/新增 | 实测枚举：化妆品、电子产品、保健用品、其他品类；【补充】分类管理页维护 |
| 长*宽*高(cm) | 3 个数字 | 计算体积重 = 长×宽×高/5000（计费用，推断） |
| 重量(kg) | 数字 | 计费用 |
| 规格 | 文本 | 包装规格/单位（实测值："1"、"件"） |
| 状态 | 开关 | 启用/禁用；禁用产品不可用于新增入库/出库/映射 |
| 审核状态 | 枚举 | 待审核/已审核/已驳回（实测值"已审核"）；新增/导入默认"待审核"，由运营方审核 |
| FnSku | 文本 | Amazon FBA 标识（筛选中支持；换标使用） |

**交互逻辑**

1. 新增：填写以上字段 + 上传图片 → 保存 → 待审核。
2. 复制：以选中行为模板新建，Sku 需重新填写。
3. 审核：客服/运营勾选 → 点击"审核" → 批量通过/驳回（驳回必填原因【补充】）。
4. 导入：下载模板 → 填写 → 上传 → 逐行校验并返回错误报告。
5. "更多"操作：编辑、禁用/启用、查看库存（推断）。

**业务规则**

1. Sku 在用户维度唯一；已产生库存/单据的产品不允许删除，仅可禁用。
2. 审核未通过的产品不可在入库/出库选择产品时使用。
3. 长宽高、重量变更仅影响后续计费，不追溯历史账单。

**验收要点**：五项筛选生效（含多 Sku 逗号查询）；审核按钮在未选中时禁用；导入错误定位到行；禁用产品在"选择产品"弹窗不可见。

#### 6.2.2 产品分类管理【补充】

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 维护产品分类字典（实测枚举：化妆品、电子产品、保健用品、其他品类），支持运营自定义 |
| 字段 | 分类名称（中/英）、排序、状态 |
| 业务规则 | 分类被产品引用时不可删除，仅可禁用；分类支持中英文双语显示 |
| 验收要点 | 新增分类后产品表单可选；禁用分类后产品新增不可选但存量显示正常 |

---

### 6.3 入库管理（/inbound/*）

#### 6.3.1 创建入库（/inbound/create）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 客户创建入库预报单（ASN），告知仓库即将到达的货物信息 |
| 表单结构 | 顶部模式切换：单选"默认"（默认选中）/ 单选"批量导入"；基本信息区 + 产品子表区 |

**表单字段（实测）**

| 字段 | 控件 | 必填 | 说明 |
| --- | --- | --- | --- |
| 用户名 | 只读文本 + "选择"按钮 | 是 | 默认当前登录账号（实测 SHEN）；管理员可切换客户 |
| 入库单号 | 只读文本 | 自动 | 系统自动生成，规则 `RK + yyyyMMddHHmmss + 3位流水`（实测样例 RK20260908102932307、RK20260827090817482） |
| 选择目的仓库 | 下拉 | 是 | 实测当前可选"威汇赢韩国仓"，架构支持多仓 |
| 跟踪号 | 文本 | 否（建议必填） | 头程物流追踪号，"便于跟踪货物轨迹"，支持逗号分隔多个 |
| 选择服务渠道 | 下拉 | 是 | 入库服务类型（如普通入库）（枚举由运营配置） |
| 选择入库类型 | 下拉 | 是 | 如整托/整箱/包裹（枚举由运营配置） |
| 选择到仓方式 | 下拉 | 是 | 如快递/卡车/自送（枚举由运营配置） |
| 预计到仓时间 | 日期时间 | 是 | 供仓库排班与计费参考 |
| 箱型 | 单选：拆单(箱) / 整单(箱) | 是（默认拆单） | 拆单=按箱逐箱上架，整单=整批上架 |
| 备注 | 多行文本 | 否 | — |
| 选择产品 | 弹窗 | 是（≥1 行） | 产品子表列：序号、产品Sku、产品名称、产品英文、预报数量、操作（删除） |
| 提交 | 按钮 | — | 校验通过后创建入库单，状态=待揽货 |

**"默认"与"批量导入"两种模式**：默认=在线逐行添加产品并填写预报数量；批量导入=下载模板（Sku、预报数量）上传，校验失败行给出原因。

**业务规则**

1. 预报数量必须为正整数；所选产品必须"已审核 + 启用"。
2. 提交后入库单号锁定不可修改；待揽货状态允许客户编辑产品明细与预报数量（推断）；揽收后禁止编辑。
3. 批量导入模式同样生成一张入库单，产品行为导入结果。
4. 同一客户同一仓库允许多张在途入库单。

**异常与边界**：所选产品被禁用（提交后）→ 提交时拦截并提示；跟踪号多值 → 逗号拆分校验非空。

**验收要点**：入库单号自动生成且符合 RK 规则；必填校验完整；产品子表增删改正常；提交后出现在"待揽货"列表；批量导入错误报告准确。

#### 6.3.2 待揽货（/inbound/pickup）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 展示状态=待揽货（已预报未揽收）的入库单列表 |
| 筛选条件 | 同"所有单"（仓库名、用户名、入库编号或追踪号多逗号、上架状态、起始/截至日期、状态） |
| 操作按钮 | 查询、导出 |
| 列表字段 | 同"所有单"（见 6.3.4） |
| 业务规则 | 仅展示"待揽货"状态；仓库在该页执行"揽收"（【补充】仓库端按钮，客户只读） |

#### 6.3.3 待确认（/inbound/confirm）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 展示已揽收、待仓库确认清点的入库单 |
| 筛选条件 | 仓库名、用户名、入库编号或追踪号（多逗号） |
| 操作按钮 | 查询 |
| 列表字段 | 序号、入库单号、用户名、仓库名、追踪号、交货方式、备注、操作（【补充】仓库端：确认/异常登记） |
| 业务规则 | 仓库确认后进入上架流程；确认时可录入实际到货差异（缺失/破损）（【补充】） |

#### 6.3.4 所有单（/inbound/all）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 全部入库单的查询与导出（核心报表页） |
| 筛选条件 | 仓库名、用户名、入库编号或追踪号（多个英文逗号 ',' 隔开）、上架状态（下拉，实测值"上架"）、起始日期、截至日期、状态（下拉"全部状态"；i18n key: forecast.filter.placeholder.status） |
| 操作按钮 | 查询、导出 |
| 列表字段 | 序号、入库单号、用户名（可排序）、仓库名（可排序）、追踪号、交货方式、备注、时间、状态、操作（查看） |

**关键字段说明**

| 字段 | 说明 |
| --- | --- |
| 入库单号 | RK+日期+流水（如 RK20260827090817482） |
| 交货方式 | 枚举实测：自发头程、中转代发 |
| 时间 | 复合展示三时间戳，格式实测："创建：2026-08-27 09:08:50 揽收：2026-08-27 09:09:53 上架：2026-08-27 09:10:02"（未发生的环节不显示） |
| 状态 | 枚举实测：已完成；全集：待揽货/待确认/上架中/已完成/已取消/无主货 |
| 查看 | 弹窗或跳转明细页：基本信息 + 产品明细（Sku、名称、预报数量、实际收货数量（【补充】）、上架数量（【补充】）） |

**业务规则**：导出列与页面列一致，时间列展开为创建时间/揽收时间/上架时间三列（建议）；状态筛选支持多选；分页 10 条/页可选。

**验收要点**：筛选条件全生效；排序正确；时间戳三段展示与实际动作一致；导出数据量准确。

#### 6.3.5 认领管理（/inbound/claim）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 仓库收到"无主货"（无预报/无法匹配）时挂出待认领列表，客户认领后并入正常入库流程 |
| 筛选条件 | 仓库名、入库编号或追踪号（多个英文逗号 ',' 隔开） |
| 操作按钮 | 查询 |
| 列表字段 | 序号、入库单号、追踪号、交货方式、备注、操作（认领） |
| 业务规则 | 1) 无主货由仓库登记（入库单号可为仓库临时编号）并挂认领；2) 客户核对追踪号等信息点击"认领" → 生成/关联本人入库单（状态进入待确认/上架流程）；3) 已认领记录从客户列表消失；4) 长期未认领货物按仓库保管条款处理（运营规则，超期提醒【补充】） |
| 验收要点 | 认领后本人入库单出现该货物；其他用户不可见不可认领；认领操作留痕（操作人/时间） |

#### 6.3.6 仓库收货与上架作业（PDA）【补充】

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 仓库操作员用 PDA/扫码枪完成揽收登记、清点确认、按库位上架，保证库存账实一致 |
| 作业流 | ① 揽收登记：扫描运单/入库单条码 → 状态"待确认"，记录揽收时间；② 清点确认：逐箱清点，录入差异（收货数量 vs 预报数量），差异>0 需仓库管理员确认；③ 扫码上架：扫描产品条码/SKU 标签 → 系统推荐库位（或扫描目标库位）→ 确认数量 → 记录上架时间，库存增加，入库单状态"已完成"；④ 打印入库标签（客户库位标签/产品标签）【补充】 |
| 数据要求 | 每次扫码写入操作流水（操作人、时间、PDA 号）；上架数量自动写入库存与库存流水（变动类型=入库上架） |
| 异常 | 扫描无预报产品 → 提示转"无主货/认领"；数量超预报 → 拦截并要求管理员确认；重复扫描 → 数量累计去重 |
| 验收要点 | 揽收/上架时间与客户端"所有单"时间列一致；库存流水变动类型与数量准确；异常流程可走通 |

---

### 6.4 一件代发（/outbound/*）

#### 6.4.1 创建出库（/outbound/create）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 客户手工创建一件代发出库单（无店铺订单场景） |
| 表单结构 | 单选"默认"/"批量导入"（实测有模式切换）；基本信息区 + 收件人区 + 产品子表区 |

**表单字段（实测）**

| 字段 | 控件 | 必填 | 说明 |
| --- | --- | --- | --- |
| 用户名 | 只读 + "选择"按钮 | 是 | 默认当前账号；管理员可代客户创建 |
| 参考号 | 文本 | 否 | 客户内部参考号，列表展示用于检索 |
| 选择仓库名称 | 下拉 | 是 | 选择后才能选产品（实测"选择产品"按钮在选择仓库前 disabled） |
| 选择国家 | 下拉 | 是 | 目的国（影响物流渠道匹配） |
| 收件人信息 | 文本组 | 是 | 收件人姓名、收件人电话、详细地址、邮编（快照存在多个无标签文本框；字段建议：姓名/电话/州省/城市/地址1/地址2/邮编/邮箱（推断）） |
| 选择产品 | 弹窗 | 是（≥1 行） | 子表列：序号、Sku、产品名称、英文名称、库存（实时）、数量、操作 |
| 提交 | 按钮 | — | 创建出库单 → 状态"待下架"，分配发货单号 |

**"批量导入"模式**：模板含参考号、仓库、国家、收件人、Sku、数量；导入多张出库单。

**业务规则**

1. 产品弹窗实时显示该仓库该用户的可库库存；数量 > 可库库存时拦截。
2. 提交时对产品行做库存占用（预占）校验；同一产品多单并发下不超卖（可用 = 在库 - 已预占）。
3. 提交后生成：订单编号（系统内部单号（推断规则，见数据模型））、发货单号（仓库作业单号）。
4. 国家/仓库组合决定可用的物流渠道（在仓库端待出货环节匹配）。

**异常与边界**：库存实时变动导致提交时不足 → 提示重新核对数量；批量导入行数上限（建议 500 行/批）。

**验收要点**：未选仓库不能选产品；库存列实时准确；提交成功出现在"待下架"；超卖场景被拦截；批量导入错误报告。

#### 6.4.2 待下架（/outbound/outbound-djh）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 已创建、待仓库拣货下架的出库单 |
| 筛选条件 | 仓库名、用户名、订单编号或参考号（多个英文逗号 ',' 隔开） |
| 操作按钮 | 查询、导出、导出打单数据 |
| 列表字段 | 序号（推断，实测此页首列为订单编号前有勾选）、订单编号、用户名（可排序）、仓库名（可排序）、参考号、发货单号、收件人、收件人电话、备注、创建时间、操作（【补充】仓库端：拣货/波次） |
| 业务规则 | "导出打单数据"导出用于批量面单打印的结构化数据；客户在此页只读 |

#### 6.4.3 待出货（/outbound/outbound-dfh）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 已下架、待打印面单并打包交运的出库单 |
| 筛选条件 | 仓库名、用户名、订单编号或参考号（多逗号）、面单打印状态（默认全部） |
| 操作按钮 | 查询、导出、打印（选中后启用） |
| 列表字段 | 序号、订单编号、用户名（可排序）、仓库名（可排序）、参考号、物流渠道、发货单号、收件人、收件人电话、备注、创建时间、单号（承运商追踪号）、打印状态、操作 |
| 业务规则 | 1) 仓库端在本页为订单匹配物流渠道并获取承运商追踪号（"单号"列）；2) "打印"批量打印面单，打印成功打印状态→已打印；3) 打印状态枚举：未打印/已打印 |
| 验收要点 | 打印按钮未选中时禁用；打印后状态即时更新；单号列为承运商追踪号并与物流商可对账 |

#### 6.4.4 已出货（/outbound/outbound-yfh）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 已交运出库单查询与追踪 |
| 筛选条件 | 仓库名、用户名、订单编号或参考号（多逗号）、发货单号（多逗号）、收件人、发货起始日期、发货截至日期、面单打印状态、选择服务商 |
| 操作按钮 | 查询、导出、打印（选中后启用，补打面单） |
| 列表字段 | 序号、订单编号、用户名（可排序）、仓库名（可排序）、参考号、物流渠道、发货单号、收件人、收件人电话、备注、发货时间、打印状态、操作（更多） |
| 业务规则 | "更多"下拉建议含：查看详情、补打面单、查看物流轨迹【补充】（对接物流商轨迹 API）；导出含发货时间与承运商单号 |

#### 6.4.5 所有单（/outbound/outbound-all）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 全部一件代发出库单的查询与导出（核心报表页） |
| 筛选条件 | 仓库名、用户名、订单编号或参考号（多逗号）、发货单号（多逗号）、收件人、发货起始日期、发货截至日期、状态（下拉"全部状态"；i18n key: order.filter.placeholder.status）、面单打印状态（默认全部）、选择服务商 |
| 操作按钮 | 查询、导出 |
| 列表字段 | 序号、订单编号、用户名（可排序）、仓库名（可排序）、物流渠道、参考号、发货单号、收件人、收件人电话、备注、创建时间、发货时间、状态、打印状态、操作（查看） |
| 状态枚举 | 待下架、待出货、已出货、已取消 |
| 验收要点 | 全部筛选组合生效；状态列与三个工作队列页数据一致；导出完整 |

#### 6.4.6 拣货/复核/打包/称重（PDA）【补充】

| 项目 | 内容 |
| --- | --- |
| 作业流 | ① 拣货：按单/按波次生成拣货任务，PDA 显示拣货库位与数量，扫码校验产品与库位 → 下架完成，状态"待下架"→"待出货"；② 复核：PDA 逐件扫描产品条码校验订单一致性；③ 打包：选择包装耗材，记录包裹信息；④ 称重：接秤自动读重，重量写入订单（计费用），异常重量（与产品档案重量偏差>阈值）提示复核；⑤ 交运：获取承运商面单+追踪号（面单获取见 6.4.7），打印面单贴包，扫描交接 → 状态"已出货"，记录发货时间 |
| 波次 | 支持按仓库+物流渠道+国家聚合生成波次拣货单；波次内逐单核销 |
| 验收要点 | 状态流转与客户端列表一致；称重数据写入账单计费；交接单可导出给承运商 |

#### 6.4.7 面单获取与打印【补充】

| 需求 | 说明 |
| --- | --- |
| 面单来源 | 物流服务商 API（预配舱/取号）返回 PDF/ZPL 面单与追踪号；无 API 渠道支持手工录单号 |
| 打印方式 | 浏览器打印（PDF）；PDA/客户端支持热敏打印（ZPL）；"导出打单数据"输出标准化 Excel/CSV 供第三方打单工具 |
| 打印状态 | 未打印/已打印；打印动作记录操作人与时间；支持补打并留痕 |
| 失败处理 | 取号失败 → 订单标注失败原因（如余额不足/地址不全），可重试；面单文件缓存 90 天（推断） |

---

### 6.5 换标管理（/fbarebrand/*）

#### 6.5.1 创建换标（/fbarebrand/create）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 客户创建 FBA 换标单：将海外仓库存的 FBA 商品换新 FnSku 标签后发往亚马逊指定仓库 |
| 表单字段（实测） | ① 选择Fba仓库（下拉）② 选择发货仓库（下拉）③ 选择派送渠道（下拉）④ 是否（下拉：否/是，语义推断为"是否优先/是否加急"或"是否本仓发货"，需与原系统确认——列入开放问题）⑤ 参考号（文本）⑥ FBA地址（文本，亚马逊仓收货地址）⑦ 货件编号（文本，Amazon Shipment ID，如 FBA15ABCD123）⑧ 亚马逊内部编号（文本）⑨ 点我上传（按钮，上传附件：装箱单/授权文件等）⑩ 选择产品（弹窗）⑪ 提交 |

**产品子表（实测列）**：序号、Sku、产品名称、FnSku、新FnSku、数量、操作（删除）。

**业务规则**

1. FnSku 默认带出产品档案中的 FnSku（可编辑）；新FnSku 必填且与旧 FnSku 不同（提交校验）。
2. 数量 ≤ 该发货仓库该用户的可库库存；提交时预占库存。
3. 换标单创建后占用库存，至"已发货"或"取消"释放/扣减。
4. 贴标物料费/换标操作费按计费规则生成账单（见第 7 章）。

**异常与边界**：上传文件类型限制（pdf/jpg/png/xlsx，单文件 ≤10MB（建议））；FBA地址必填校验（Amazon 地址格式）。

**验收要点**：必填校验；子表增删改；提交后出现在"待拣货"；库存预占生效。

#### 6.5.2 待拣货（/fbarebrand/rebrand-djh，菜单名"待下架"）

| 项目 | 内容 |
| --- | --- |
| 筛选条件 | 用户名、订单编号或追踪号 |
| 列表字段 | 订单编号、用户名、参考号、备注、创建时间、操作（【补充】仓库端：拣货下架） |
| 业务规则 | 仓库拣货完成后流转到"待贴标"；客户只读 |

#### 6.5.3 待贴标（/fbarebrand/rebrand-dtb）

| 项目 | 内容 |
| --- | --- |
| 筛选条件 | 用户名、订单编号或追踪号 |
| 列表字段 | 订单编号、用户名、参考号、备注、创建时间 |
| 业务规则 | 仓库按"新FnSku"打印标签并逐件贴标；贴标完成流转"待发货"（【补充】仓库端操作；建议贴标数量录入与剩余件数校验） |

#### 6.5.4 待发货（/fbarebrand/rebrand-dfh）

| 项目 | 内容 |
| --- | --- |
| 筛选条件 | 用户名、订单编号或追踪号 |
| 列表字段 | 订单编号、用户名、参考号、备注、创建时间 |
| 业务规则 | 按创建时选定的"派送渠道"交运；交运后流转"已发货"并记录发货时间 |

#### 6.5.5 已发货（/fbarebrand/rebrand-yfh）

| 项目 | 内容 |
| --- | --- |
| 筛选条件 | 用户名、订单编号或追踪号 |
| 列表字段 | 订单编号、用户名、参考号、备注、创建时间、操作（查看） |
| 业务规则 | 记录发货时间；支持查看交运单号（【补充】列扩展：承运商单号/派送渠道/发货时间） |

#### 6.5.6 换标仓库作业【补充】

PDA/网页端：拣货 → 打印新 FnSku 标签（批量）→ 贴标登记（件数核对）→ 打包装箱 → 按派送渠道交运。每次操作留痕；贴标错误（扫描 FnSku 与任务不符）即时拦截。

---

### 6.6 退货管理（/return/*）

#### 6.6.1 创建退货（/return/create）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 客户创建退货预报：终端买家/平台退回的货物回运海外仓处理 |
| 表单字段（实测） | ① 选择目的仓库（下拉）② 跟踪号（文本，"便于跟踪货物轨迹"）③ Sku（文本）④ 选择服务渠道（下拉）⑤ 选择入库类型（下拉）⑥ 货物类型（下拉；如退货换标、退货重新上架、销毁等（枚举推断））⑦ 离港时间（日期时间）⑧ 到港时间（日期时间）⑨ 预计到仓时间（日期时间）⑩ 备注（文本）⑪ 选择产品（弹窗）⑫ 提交 |

**产品子表（实测列）**：序号、产品Sku、产品名称、产品英文、预报数量、操作。

**业务规则**

1. 提交后生成退货单号（列实测存在"退货单号"，规则推断见数据模型）。
2. 货物类型决定后续"退货待处理"的默认处置方式（重新上架入可库库存/销毁/退回卖家）。
3. 离港/到港时间为卖家填写的运输节点时间，仅供仓库参考与超期跟踪。

**验收要点**：必填校验；提交后出现在"退货待揽收"；单号规则稳定唯一。

#### 6.6.2 退货待揽收（/return/return-dlh）

| 筛选条件 | 用户名、订单编号或追踪号 |
| --- | --- |
| 列表字段 | 序号、用户名（可排序）、入库单号（可排序）、追踪号（可排序）、退货单号、参考号（可排序）、类型、备注、操作 |
| 业务规则 | 列表同时展示关联入库单号——退货回运到仓按入库单走揽收流程；仓库揽收后流转"退货待上架" |

#### 6.6.3 退货待上架（/return/return-dsj）

| 筛选条件 | 用户名、订单编号或追踪号 |
| --- | --- |
| 列表字段 | 同"退货待揽收"（含入库单号） |
| 业务规则 | 仓库 PDA 扫码上架（【补充】）；上架完成库存增加，流转"退货待处理" |

#### 6.6.4 退货待处理（/return/return-dcl）

| 筛选条件 | 用户名、订单编号或追踪号 |
| --- | --- |
| 列表字段 | 序号、用户名（可排序）、退货单号（可排序）、追踪号（可排序）、参考号（可排序）、类型、备注、操作 |
| 业务规则 | 仓库/客服按"货物类型"执行处置：重新上架、换标、销毁、拍照反馈卖家（【补充】拍照上传）；处置完成流转"退货待完成" |

#### 6.6.5 退货待完成（/return/return-dwc）

| 筛选条件 | 用户名、订单编号或追踪号 |
| --- | --- |
| 列表字段 | 同"退货待处理" |
| 业务规则 | 处置结果复核确认（库存增加/销毁登记）→ 客户或客服点击完成 → 流转"退货已完成" |

#### 6.6.6 退货已完成（/return/return-ywc）

| 筛选条件 | 用户名、订单编号或追踪号、退货完成起始日期、退货完成截至日期 |
| --- | --- |
| 列表字段 | 序号、用户名（可排序）、退货单号（可排序）、追踪号（可排序）、参考号（可排序）、类型、备注、退货完成时间 |
| 业务规则 | 报表页，只读；退货完成时间用于 SLA 统计 |

#### 6.6.7 退货仓库作业【补充】

PDA：揽收（扫回程运单，关联退货单）→ 上架（质检分级：可售/不可售）→ 处置执行（重新上架入可库库存或销毁登记）→ 完成。所有数量变化写入库存流水（变动类型=退货上架/退货销毁）。

---

### 6.7 库存管理（/stock/*）

#### 6.7.1 库存明细（/stock/stock-detail，库存流水）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 展示库存变动流水（账），可追溯每一笔库存变化的来源单据 |
| 筛选条件 | 仓库名、用户名、Sku/Fnsku、产品名称、变动时间开始、变动时间截止 |
| 操作按钮 | 查询、导出 |
| 列表字段 | 序号、仓库名称、用户名、Sku、产品名称、英文名称、数量（变动数量，正增负减）、变动类型、备注 |

**变动类型枚举（必须覆盖）**：入库上架（+）、一件代发出库（-）、换标出库（-）/换标回库（+）、退货上架（+）、盘点调整（±）、库存调整（±）、库存移位（0，换库位）（【补充】项标注补充）、认领入库（+）。

**业务规则**

1. 每笔流水必须可追溯到来源单据（入库单/出库单/换标单/退货单/盘点单编号），备注列展示来源单号（建议）。
2. 流水为只读，任何库存变化必须产生流水（强一致）。
3. 时间筛选默认近 30 天（建议）；导出遵循筛选。

**验收要点**：任意 SKU 的 期初+流水=当前库存 可对账；变动类型枚举完整；导出准确。

#### 6.7.2 库存查询（/stock/inquiry，实时库存）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 当前可用库存实时查询（卖家最常用页） |
| 筛选条件 | 仓库名、用户名、Sku/Fnsku、产品名称 |
| 操作按钮 | 查询、导出 |
| 列表字段 | 序号、仓库名称、用户名、Sku、产品图片、产品名称、英文名称、分类、规格、可库库存 |
| 业务规则 | 1) 可库库存 = 在库库存 - 出库/换标预占（定义见数据模型）；2) 数据按 用户×仓库×SKU 聚合；3) 【补充】建议扩展列：在库总量、预占数量、待入库数量（在途预报）；4) 缓存策略：实时查询不缓存（延迟 <1s） |
| 验收要点 | 出库下单时看到的"库存"列与本页数值一致；上架/出货后数值秒级更新 |

#### 6.7.3 库位管理【补充】

| 需求 | 说明 |
| --- | --- |
| 库位编码 | 支持 区-巷道-货架-层-位 五级编码（如 A-01-02-03）；支持库位类型（存储位/拣货位/暂存位/退货位） |
| 功能 | 库位新增/编辑/停用；库位库存查询（点库位看 SKU 清单）；SKU 与库位绑定关系（一货多位/一位多货均支持） |
| 与 PDA 联动 | 上架时推荐空库位；拣货时按库位出拣货路径 |
| 验收要点 | 库位 CRUD；库位库存与 SKU 库存一致 |

#### 6.7.4 盘点/调整/移位【补充】

| 需求 | 说明 |
| --- | --- |
| 盘点 | 创建盘点单（按仓库/库位/SKU 范围）→ PDA 实盘录入 → 差异表（系统数 vs 实盘数）→ 仓库管理员审批 → 差异生成库存调整流水（变动类型=盘点调整），留审批记录 |
| 调整 | 有权限角色手工调整库存（如破损报损），必填原因，生成"库存调整"流水 |
| 移位 | PDA 扫 SKU → 扫目标库位 → 移位记录（不改变总量，仅库位归属变化） |
| 验收要点 | 盘点闭环（创建→实盘→审批→过账）；调整/移位全部有流水与操作人 |

#### 6.7.5 库存预警【补充】

SKU 可配置安全库存上下限；低于下限时向客户消息中心推送预警，并支持报表导出；【二期】。

---

### 6.8 财务中心（/finance/*）

#### 6.8.1 银行账户（/finance/bankInfo）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 客户维护收款/付款银行账户信息（表单页，实测 6 个文本输入 + "保存"按钮） |
| 建议字段 | 户名、开户行、银行账号、开户支行、SWIFT/IBAN（境外收款（推断））、币种（默认 CNY） |
| 业务规则 | 1) 每客户一套默认账户（单条记录保存）；2) 银行账号展示脱敏（后 4 位），完整信息仅财务角色可见；3) 修改留痕（审计日志） |
| 验收要点 | 保存成功提示；再次进入回显；脱敏显示；修改留痕 |

#### 6.8.2 订单账单（/finance/payment）

| 项目 | 内容 |
| --- | --- |
| 页面目的 | 展示按业务单据生成的费用账单，客户核对费用 |
| 筛选条件 | 用户名、关联单号 |
| 操作按钮 | 查询、（勾选后）批量操作 |
| 列表字段 | 序号、账单编号（可排序）、仓库名、用户名、关联单号（可排序）、费用金额(￥)（可排序）、费用时间（可排序）、备注、状态、操作 |

| 字段 | 说明 |
| --- | --- |
| 账单编号 | 账单唯一号（规则推断见数据模型） |
| 关联单号 | 来源业务单据号（RK 入库单号/出库订单编号/换标单号/退货单号） |
| 费用金额(￥) | 人民币金额，两位小数 |
| 费用时间 | 账单生成/计费发生时间 |
| 状态 | 枚举推断：待支付/已支付（已扣费）/已取消；实测列表存在"状态"列 |
| 操作 | 查看明细；批量勾选支持批量支付/批量导出（推断） |

**业务规则**

1. 账单由系统按计费规则自动生成（见第 7 章），也支持财务手工添加/调整（留痕）。
2. 已支付/已扣费账单不可删除；错误账单走"红冲"生成负数账单（建议）。
3. 客户只能看到本人账单；导出遵循筛选。

**验收要点**：计费事件触发后账单分钟级出现；金额与计费规则一致；批量操作仅对"待支付"生效。

#### 6.8.3 计费规则配置（/finance/billing-rule）【补充】

| 需求 | 说明 |
| --- | --- |
| 计费项目 | 入库操作费（按件/按箱）、仓储费（按天/按月、按体积重或件数）、出库操作费（按单/按件/按重量段）、换标费（贴标费/物料费）、退货处理费、认领费、加急费等 |
| 规则维度 | 仓库 + 客户等级/客户 + 服务渠道；生效时间区间；计价方式（固定/阶梯/按实际称重） |
| 最低消费 | 每单/每月最低收费 |
| 验收要点 | 规则配置后新业务单据按新规则计费；历史账单不追溯；规则冲突时按优先级（客户>仓库>全局）取值 |

#### 6.8.4 充值与余额【补充】（推断）

实测系统未见充值页，但账单"状态"列 + 客户账号运营模式（VIP 前缀）强烈暗示"预充值扣费"模式（推断）。建议：客户充值（财务确认到账）→ 余额增加；账单生成时自动扣费 → 余额减少；余额不足 → 账单待支付 + 欠费提醒 + （可选）限制新单提交；提供余额流水页。

---

## 7. 计费与财务（汇总）

### 7.1 计费事件与计费项目对照

| 业务节点（实测状态） | 计费事件 | 计费项目 | 计费基础 |
| --- | --- | --- | --- |
| 入库"已完成"（上架完成） | 入库上架 | 入库操作费 | 实际上架件数/箱数 |
| 每日/每月（在库） | 定时任务 | 仓储费 | SKU×体积重×天数（或件数×天数） |
| 出库"已出货" | 出库交运 | 出库操作费 + 物流费 | 件数/重量（PDA 称重数据）/承运商账单 |
| 换标"已发货" | 换标完成 | 换标费（贴标+物料） | 贴标件数 |
| 退货"已完成" | 退货处理 | 退货处理费 | 处置件数/方式 |
| 认领完成 | 认领 | 认领操作费 | 次 |
| 加急服务 | 下单时 | 加急费 | 次 |

注：物流费建议优先采用物流商 API 账单导入比对（对账差异报表【补充】）。

### 7.2 账单生命周期

```
计费事件 → 账单生成（待支付） → 自动扣费（余额充足）→ 已支付
                                └→ 余额不足 → 保持待支付 + 欠费提醒
          → 财务手工调整（补录/红冲）→ 版本留痕
          → 对账（客户确认/财务核销）
```

### 7.3 对账与报表【补充】

- 月度对账单：按客户汇总各计费项目金额，附明细，导出 Excel。
- 应收报表、余额报表、欠费客户清单。
- 物流费对账：系统预估物流费 vs 承运商账单，差异超阈值预警。

---

## 8. 集成能力

### 8.1 电商平台对接（店铺授权 + 订单同步）

| 需求 | 说明 |
| --- | --- |
| 平台范围 | Amazon SP-API 优先（实测系统有 FnSku/货件编号/FBA 地址，明确含 Amazon）；预留 Shopee/Coupang/11Street/Lazada 等扩展接口（韩国仓场景）【补充】 |
| 授权 | OAuth 2.0 授权跳转（SP-API）+ 手动填入凭据两种方式；token 刷新与失效监控，失效时消息提醒客户重新授权 |
| 订单同步 | 增量拉取（按下单时间/更新时间游标）；幂等（平台订单号唯一）；字段：平台订单号、店铺、下单时间、收件人、商品（平台 Sku×数量）、金额 |
| 订单状态同步 | 平台取消 → 未审核订单自动取消；已审核订单人工处理（提示） |
| 同步频率 | 定时 30 分钟（可配置）+ 手动"同步订单"按钮 |

### 8.2 SKU 映射

见 6.1.3。映射引擎规则：店铺订单商品行 → 按"店铺+平台产品Sku"查映射 → 生成系统 Sku×数量 → 数量字段"系统Sku数量"支持 1:N 拆件。映射失败订单进入待处理队列，支持"重新执行映射"。

### 8.3 物流服务商对接（面单/Tracking）

| 需求 | 说明 |
| --- | --- |
| 渠道管理 | 【补充】物流服务商/渠道基础数据维护（服务商名称、渠道、计费模式、支持目的国）——对应出库列表"选择服务商""物流渠道"筛选项 |
| 取号 | 出库"待出货"环节调用服务商 API 获取面单 + 承运商追踪号（"单号"列） |
| 轨迹 | 【补充】订阅承运商轨迹推送或轮询，"已出货"订单展示物流轨迹 |
| 对账 | 【补充】服务商账单导入比对 |

### 8.4 开放 API【补充】（推断）

为 ERP/卖家系统提供开放接口（鉴权 AppKey/AppSecret + 签名）：产品同步、库存查询、入库单创建/查询、出库单创建/查询、账单查询。版本化 /api/v1/*，限流与审计。列入三期。

---

## 9. 非功能需求

| 类别 | 需求 | 指标/规则 |
| --- | --- | --- |
| 性能 | 列表查询响应 | P95 ≤ 2s（百万级订单表，常规筛选条件）；导出 1 万行 ≤ 30s（异步导出（建议）） |
| 性能 | 并发 | 支持 500 并发登录用户、100 TPS 单据写入（峰值大促） |
| 性能 | 库存一致性 | 库存扣减/预占为事务操作，杜绝超卖；流水与余额强一致 |
| 可用性 | 系统可用率 | ≥ 99.5%（月度）；核心链路（库存查询、出库下单）故障降级提示 |
| 安全 | 登录 | 图形验证码防爆破；连续失败 N 次锁定（建议 5 次/15 分钟）；密码加盐哈希存储 |
| 安全 | 会话 | "12小时内免登录"= 12h 滑动/固定 Token；过期强制重登；登出即失效 |
| 安全 | 权限 | RBAC（角色-权限点-菜单/按钮三级）；接口级鉴权（按钮禁用≠安全）；越权访问返回 403 |
| 安全 | 数据隔离 | 多租户按"用户名"维度强制隔离（所有查询隐式拼接 user_id）；仓库角色按仓库隔离 |
| 安全 | 传输与存储 | 全站 HTTPS；敏感字段（银行账号）脱敏展示、加密存储 |
| 安全 | 审计 | 登录日志、单据状态变更日志、库存调整/账单修改日志（操作人/时间/IP/前后值） |
| i18n | 多语言 | 中/英双语；i18n key 全覆盖（杜绝实测出现的 forecast.filter.placeholder.status、order.filter.placeholder.status 漏翻问题）；时间/金额本地化格式 |
| 兼容性 | 浏览器 | Chrome/Edge 最新两个大版本；最低分辨率 1280×768；表格横向滚动 |
| 可维护性 | 单号可配置 | 单号规则（前缀/日期格式/流水位数）支持配置化，保证唯一性 |
| 数据 | 备份 | 数据库每日全备 + 实时增量；核心单据逻辑删除不物理删 |

---

## 10. 数据模型概要

### 10.1 核心实体清单

| 实体 | 关键字段 | 说明 |
| --- | --- | --- |
| 用户 User | user_id、用户名（VIP0157/SHEN）、密码、角色、状态、语言偏好 | 多租户根；"用户名"是全系统数据隔离与展示键 |
| 角色/权限 Role/Permission【补充】 | role_id、permission_code | RBAC |
| 仓库 Warehouse | warehouse_id、仓库名（威汇赢韩国仓）、国家、地址、状态 | 多仓多国家 |
| 产品 Product | product_id、user_id、Sku（用户内唯一）、FnSku、中文名、英文名、图片、分类 category_id、长/宽/高(cm)、重量、规格、状态（启用/禁用）、审核状态（待审核/已审核/已驳回） | 产品主档 |
| 产品分类 Category【补充】 | category_id、名称（中/英）、排序、状态 | 实测枚举：化妆品/电子产品/保健用品/其他品类 |
| 店铺 Shop | shop_id、user_id、平台、店铺名称、英文名称、排序、备注、授权凭据/Token、授权状态、失效时间【补充】 | 店铺授权 |
| 平台订单 PlatformOrder | order_id、user_id、shop_id、平台订单号（唯一）、状态、下单时间、收件人信息、金额 | 店铺订单 |
| 平台订单商品 PlatformOrderItem | item_id、order_id、平台产品Sku、数量、金额 | — |
| SKU映射 SkuMapping | mapping_id、user_id、shop_id、平台产品Sku、系统Sku、系统Sku数量、状态、创建时间 | 平台Sku→系统Sku（1:N 行） |
| 入库单 InboundOrder | inbound_id、入库单号（RK+yyyyMMddHHmmss+3位流水）、user_id、warehouse_id、追踪号（多值）、交货方式（自发头程/中转代发）、服务渠道、入库类型、到仓方式、预计到仓时间、箱型（拆单/整单）、状态、创建时间、揽收时间、上架时间、备注、取消时间/操作人 | ASN |
| 入库单明细 InboundItem | item_id、inbound_id、product_id、预报数量、实际收货数量【补充】、上架数量【补充】 | — |
| 认领记录 Claim | claim_id、warehouse_id、入库单号（仓库临时）、追踪号、交货方式、备注、状态（待认领/已认领）、认领人/认领时间 | 无主货 |
| 出库单 OutboundOrder | outbound_id、订单编号、user_id、warehouse_id、目的国家、参考号、收件人（姓名/电话/州省/城市/地址1/地址2/邮编/邮箱）、物流渠道/服务商、发货单号、承运商单号、称重【补充】、面单文件、打印状态（未打印/已打印）、状态（待下架/待出货/已出货/已取消）、创建时间、发货时间、备注 | 一件代发；来源=手工创建/店铺订单审核 |
| 出库单明细 OutboundItem | item_id、outbound_id、product_id、数量、下单时库存快照 | — |
| 换标单 RebrandOrder | rebrand_id、订单编号、user_id、Fba仓库、发货仓库、派送渠道、是否、参考号、FBA地址、货件编号、亚马逊内部编号、附件、状态（待拣货/待贴标/待发货/已发货/已取消）、创建时间、发货时间、备注 | — |
| 换标单明细 RebrandItem | item_id、rebrand_id、product_id、Sku、产品名称、FnSku、新FnSku、数量 | — |
| 退货单 ReturnOrder | return_id、退货单号、user_id、warehouse_id、关联入库单号、追踪号、参考号、类型（货物类型）、Sku、服务渠道、入库类型、离港时间、到港时间、预计到仓时间、状态（待揽收/待上架/待处理/待完成/已完成/已取消）、退货完成时间、备注 | — |
| 退货单明细 ReturnItem | item_id、return_id、product_id、预报数量、实际数量【补充】、处置结果【补充】 | — |
| 库位 Location【补充】 | location_id、warehouse_id、编码（区-巷道-货架-层-位）、类型、状态 | — |
| 库存 Inventory | inventory_id、user_id、warehouse_id、product_id、location_id【补充】、在库数量、预占数量 | 可库库存=在库-预占；唯一键（user+warehouse+product[+location]） |
| 库存流水 InventoryLedger | ledger_id、user_id、warehouse_id、product_id、变动数量（±）、变动后数量、变动类型（入库上架/出库/换标/退货上架/盘点调整/库存调整/移位/认领入库）、来源单据类型+单号、备注、创建时间 | 只追加不可改 |
| 盘点单 StockCount【补充】 | count_id、warehouse_id、范围、状态（进行中/待审批/已过账）、明细（SKU、系统数、实盘数、差异）、审批人 | — |
| 账单 Bill | bill_id、账单编号、user_id、warehouse_id、关联单据类型+关联单号、费用项目、费用金额(￥)、费用时间、状态（待支付/已支付/已取消）、支付方式、备注 | — |
| 银行账户 BankAccount | bank_id、user_id、户名、开户行、支行、账号（加密）、SWIFT/IBAN、币种 | — |
| 计费规则 BillingRule【补充】 | rule_id、费用项目、仓库、客户范围、计价方式（固定/阶梯/重量段）、价格、生效起止、优先级 | — |
| 物流服务商/渠道 Carrier/Channel【补充】 | carrier_id、服务商名称、渠道、支持目的国、取号方式 | 对应筛选"选择服务商""物流渠道" |
| 操作日志 AuditLog【补充】 | log_id、操作人、模块、对象、动作、前后值、IP、时间 | — |

### 10.2 实体关系（文字化 ER）

```
User 1─N Shop 1─N PlatformOrder 1─N PlatformOrderItem
User 1─N Product 1─N SkuMapping（Shop+平台Sku → Product）
User 1─N InboundOrder 1─N InboundItem ─N Product
User 1─N OutboundOrder 1─N OutboundItem ─N Product；OutboundOrder N─1 Warehouse；OutboundOrder ─N Bill
User 1─N RebrandOrder 1─N RebrandItem ─N Product
User 1─N ReturnOrder 1─N ReturnItem ─N Product；ReturnOrder ─1 InboundOrder（回运入库）
User+Warehouse+Product → Inventory 1─N InventoryLedger；Warehouse 1─N Location
User 1─1 BankAccount；User 1─N Bill；BillingRule N─1 Warehouse
```

### 10.3 单号规则汇总

| 单据 | 规则 | 样例/依据 |
| --- | --- | --- |
| 入库单号 | `RK` + `yyyyMMddHHmmss` + 3 位流水，全局唯一 | 实测 RK20260827090817482、RK20260908102932307 |
| 出库订单编号 | `CK` + `yyyyMMddHHmmss` + 3 位流水（推断，与入库对齐；需产品确认） | 三个号码体系之一 |
| 出库发货单号 | 仓库内部作业单号（推断由仓库端生成） | 列表独立列 |
| 承运商单号 | 物流服务商取号返回的追踪号 | "待出货/已出货"页"单号"列 |
| 换标订单编号 | `HB` + 日期 + 流水（推断） | 列表"订单编号"列 |
| 退货单号 | `TH` + 日期 + 流水（推断） | 退货列表"退货单号"列 |
| 账单编号 | `ZD` + 日期 + 流水（推断） | 账单列表"账单编号"列 |
| 盘点单号【补充】 | `PD` + 日期 + 流水 | — |

所有单号要求：全局唯一、含日期便于人工识别、生成原子性（并发不重号）、规则可配置。

---

## 11. 状态机汇总表

| 单据 | 状态全集 | 流转路径 | 允许取消的状态 | 终态 |
| --- | --- | --- | --- | --- |
| 入库单 | 待揽货 → 待确认 → 上架中 → 已完成；无主货（认领中）；已取消 | 创建→待揽货；仓库揽收→待确认；仓库确认→上架中；PDA 上架完成→已完成；无法匹配→无主货→认领→待确认 | 待揽货 | 已完成 / 已取消 |
| 出库单（一件代发） | 待下架 → 待出货 → 已出货；已取消 | 创建→待下架；仓库下架→待出货；交运→已出货（记录发货时间+承运商单号） | 待下架 | 已出货 / 已取消 |
| 换标单 | 待拣货 → 待贴标 → 待发货 → 已发货；已取消 | 创建→待拣货；拣货→待贴标；贴标→待发货；交运→已发货 | 待拣货 | 已发货 / 已取消 |
| 退货单 | 待揽收 → 待上架 → 待处理 → 待完成 → 已完成；已取消 | 创建→待揽收；揽收→待上架；上架→待处理；处置→待完成；确认→已完成（记录退货完成时间） | 待揽收 | 已完成 / 已取消 |
| 账单 | 待支付 → 已支付；已取消；（红冲：原单已取消+负数新单） | 计费事件→待支付；扣费/核销→已支付 | 待支付（作废） | 已支付 / 已取消 |
| 产品 | 启用/禁用；审核：待审核→已审核/已驳回 | 新增→待审核→已审核；禁用/启用随时（终态单据不受影响） | — | — |
| 店铺授权 | 已授权/已失效【补充】 | 授权成功→已授权；token 失效→已失效（重新授权→已授权） | — | — |

---

## 12. 验收标准汇总（按模块）

| 模块 | 核心验收要点 |
| --- | --- |
| 全局框架 | 登录（验证码/12h 免登录）通过；多标签页/面包屑/i18n 全覆盖无漏翻 key；通用列表（分页/排序/多选/导出）达标；数据隔离（跨用户零可见） |
| 我的店铺 | 店铺授权 CRUD+状态；订单同步幂等、可手动+定时；SKU 映射四筛选+导入导出+批量删除；未映射订单不可审核；审核后自动生成出库单 |
| 基本信息 | 产品 CRUD/复制/导入/导出；五项筛选（含多 Sku 逗号查询）；审核流（待审核→已审核/驳回）；禁用产品不可用于新单 |
| 入库管理 | RK 单号规则；创建表单全字段（含拆单/整单、默认/批量导入）；三时间戳（创建/揽收/上架）准确记录与展示；待揽货→待确认→已完成状态机走通；无主货认领闭环；导出一致 |
| 一件代发 | 未选仓库不能选产品；库存实时校验防超卖；三号码体系（订单编号/发货单号/承运商单号）；打印状态流转与批量打印；"导出打单数据"；四个队列页与所有单数据一致；发货时间记录 |
| 换标管理 | 换标表单全字段（含货件编号/亚马逊内部编号/上传）；FnSku→新FnSku 校验；待拣货→待贴标→待发货→已发货流转；库存预占与扣减 |
| 退货管理 | 退货预报表单（含离港/到港时间）；五段状态机走通；退货完成时间记录；与关联入库单联动 |
| 库存管理 | 流水与实时库存严格对账（期初+流水=当前）；变动类型枚举完整可溯源；可库库存=在库-预占；【补充】库位/盘点/调整/移位闭环 |
| 财务中心 | 计费事件自动生成账单（关联单号/金额￥/状态）；批量操作；银行账户脱敏+留痕；【补充】计费规则配置生效、余额扣费模型 |
| 集成 | Amazon 授权+订单同步；SKU 映射引擎；物流商取号（面单+追踪号）；【补充】轨迹、账单比对 |
| 非功能 | P95≤2s、并发指标、安全项（验证码/RBAC/隔离/审计）、i18n、兼容性 |

---

## 13. 里程碑建议（分期交付）

### 一期 MVP（目标：核心仓储代发闭环上线，约 8-10 周）

| 范围 | 内容 |
| --- | --- |
| 框架 | 登录（验证码/免登录）、RBAC、多标签页、i18n、通用列表组件、消息中心 |
| 基础数据 | 产品管理（含审核/导入导出）、产品分类、仓库基础数据、服务商/渠道基础数据【补充】 |
| 入库 | 创建入库（默认+批量导入）、待揽货/待确认/所有单、简易仓库端确认与上架（Web 端先替 PDA）、认领管理 |
| 出库 | 创建出库（含库存实时校验）、待下架/待出货/已出货/所有单、仓库端下架/打包/交运、面单打印（PDF）、导出打单数据 |
| 库存 | 库存查询（可库库存）、库存明细（流水） |
| 财务 | 计费规则基础版（入库/出库/仓储费）、订单账单自动生成、银行账户、手工调整 |

### 二期（目标：电商协同 + 全业务流 + PDA，约 8-10 周）

| 范围 | 内容 |
| --- | --- |
| 我的店铺 | 店铺授权（Amazon SP-API）、店铺订单同步、SKU映射、订单审核转出库单 |
| 换标 | 换标全流程（创建→已发货） |
| 退货 | 退货全流程（创建→已完成） |
| 仓库端 PDA | 收货上架、拣货复核打包称重、面单热敏打印、波次拣货 |
| 库存 | 库位体系、盘点/调整/移位 |
| 财务 | 换标费/退货费/认领费计费、余额充值扣费模型、月度对账单 |

### 三期（目标：开放与精细化）

- 开放 API（产品/库存/单据/账单）；
- 更多平台对接（Coupang/Shopee 等）与更多物流商；
- 物流轨迹订阅与展示、物流费账单比对；
- 库存预警、数据报表中心（作业效率/库存周转/费用分析）；
- 多币种计费、多语言扩展。

---

## 14. 风险与开放问题清单

### 14.1 风险

| # | 风险 | 影响 | 缓解措施 |
| --- | --- | --- | --- |
| R1 | 对标系统部分页面字段在快照中无标签（银行账户 6 个输入框、出库收件人多个文本框） | 字段定义可能偏差 | 上线前与业务方按第 6.8.1/6.4.1 建议字段逐项确认 |
| R2 | 账单"状态"列枚举与扣费模型为推断 | 财务模型返工 | 一期先实现"账单展示+手工核销"，扣费模型在二期灰度 |
| R3 | 计费规则原系统不可见 | 计费准确性争议 | 计费规则模块作为一期关键交付，配计费试算工具供客户核对 |
| R4 | 平台 API 限额（SP-API rate limit） | 订单同步延迟 | 增量同步 + 指数退避 + 手动触发兜底 |
| R5 | 库存并发扣减（多端同时操作） | 超卖 | 预占机制 + 数据库行锁/乐观锁 + 对账任务 |
| R6 | 原系统存在 i18n 漏翻 key（实测） | 国际化不完整 | 建 CI 检查：i18n key 覆盖率门禁 |
| R7 | 无主货/认领流程的责任界定 | 客诉 | 认领操作留痕 + 超期处理条款写入服务协议 |

### 14.2 开放问题（需用户/业务方确认）

| # | 问题 | 影响范围 |
| --- | --- | --- |
| Q1 | 换标单"是否（否/是）"字段的准确业务语义（加急？本仓发货？） | 换标模块 |
| Q2 | 出库订单编号的生成规则（推断 CK+时间戳+流水，需与原系统核对） | 单号体系 |
| Q3 | 账单状态的完整枚举与支付方式（预充值扣费 or 线下打款核销，或两者并存） | 财务模块 |
| Q4 | 银行账户表单 6 个字段的确切定义 | 财务模块 |
| Q5 | 店铺订单"审核"后生成出库单的目标仓库、物流渠道如何确定（店铺级默认配置？） | 店铺→出库链路 |
| Q6 | 入库"上架状态"筛选（实测值"上架"）的确切枚举（未上架/上架中/已上架？） | 入库查询 |
| Q7 | 退货"货物类型"与"类型"列的枚举全集 | 退货模块 |
| Q8 | 服务渠道、入库类型、到仓方式三个下拉的枚举全集 | 入库/退货表单 |
| Q9 | 一期是否需要保留原系统"管理员可切换客户代下单"（实测创建入库/出库表单用户名带"选择"按钮） | 权限设计 |
| Q10 | 面单打印的硬件形态（浏览器打印 / 热敏打印机 / 第三方打单工具） | 打印方案 |

---

## 附录 A：实测单据样例（供开发对照）

| 数据项 | 实测值 |
| --- | --- |
| 入库单号 | RK20260827090817482 / RK20260908102932307 |
| 入库时间列 | 创建：2026-08-27 09:08:50 揽收：2026-08-27 09:09:53 上架：2026-08-27 09:10:02 |
| 交货方式 | 自发头程 / 中转代发 |
| 入库状态 | 已完成 |
| 客户账号 | VIP0157 / SHEN |
| 仓库 | 威汇赢韩国仓 |
| 产品样例 | Sku=S0007，中文名=眼影盘，英文名=팔레트/02，分类=化妆品，长*宽*高=1*1*1，重量=1，规格=1，状态=启用，审核状态=已审核 |
| 产品分类枚举 | 化妆品、电子产品、保健用品、其他品类 |
| 未翻译 i18n key | forecast.filter.placeholder.status、order.filter.placeholder.status |
| 分页 | 10条/页（可选其他档位） |
| 币种 | 人民币 ￥ |

## 附录 B：术语英文对照（i18n 词典种子）

| 中文 | 英文 |
| --- | --- |
| 入库管理 | Inbound Management |
| 认领管理 | Claim Management |
| 创建入库 | Create Inbound |
| 待揽货 | Awaiting Pickup |
| 待确认 | Pending Confirmation |
| 一件代发 | Dropshipping |
| 创建出库 | Create Outbound |
| 待下架 | Pending Picking |
| 待出货 | Pending Shipment |
| 已出货 | Shipped |
| 换标管理 | FBA Rebrand |
| 待拣货 | Pending Picking |
| 待贴标 | Pending Relabeling |
| 待发货 | Pending Dispatch |
| 已发货 | Dispatched |
| 退货管理 | Return Management |
| 退货待揽收 | Return: Awaiting Pickup |
| 退货待上架 | Return: Awaiting Shelving |
| 退货待处理 | Return: Pending Processing |
| 退货待完成 | Return: Pending Completion |
| 退货已完成 | Return: Completed |
| 库存明细 | Inventory Ledger |
| 库存查询 | Inventory Inquiry |
| 银行账户 | Bank Account |
| 订单账单 | Order Billing |
| 可库库存 | Available Stock |

---

（完）
